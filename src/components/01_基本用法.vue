<script setup lang="ts">
let x: string
console.log(x)

// 类型推断
// 1.函数返回值类型通常不写，因为有TS的类型推断

// TS编译
// 将TS代码转为JS代码，因为JavaScript 的运行环境（浏览器和 Node.js）不认识 TypeScript 代码，否则无法运行
// TypeScript 的类型检查只是编译时的类型检查，而不是运行时的类型检查。一旦代码编译为 JavaScript，运行时就不再检查类型了。
// 编译过程其实就是将类型全部删除
</script>

<template></template>

<style scoped lang="less"></style>
